Read all Readmes Inside.
Two (2) things:

soap webservice(jax-ws-wsdl) handles exception. 
(1) It throws exception. Client sees Soap Exception on the screen. 
(2) It also sends a Fault bean (a java bean) with message and error code across wire to the client.
(3) With static block of environmental value both client side and server side xml packets and logged in plain text to examine in times of error debugging

